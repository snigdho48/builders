/** Draft-aligned label suffixes for bilingual form fields. */
export function en(label: string): string {
  return `${label} (English)`
}

export function bn(label: string): string {
  return `${label} (বাংলা)`
}
