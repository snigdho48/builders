# Optional: Google Maps JavaScript API browser key for interactive plot maps (booking, staff/investor modals, plot selector).
# Enable "Maps JavaScript API" in Google Cloud Console for this key.
# If unset or load fails, maps fall back to Leaflet + OpenStreetMap automatically.
VITE_GOOGLE_MAPS_API_KEY=
